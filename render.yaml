services:
  - type: web
    plan: free
    name: mysite
    runtime: python
    buildCommand: "./build.sh"
    startCommand: "python -m gunicorn srabon_s_backend.asgi:application -k uvicorn.workers.UvicornWorker"
